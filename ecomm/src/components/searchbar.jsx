import React from 'react';
import SearchBar from 'material-ui-search-bar'

function SearchBarReact(props) {
  return(
    <SearchBar
      style={{
        margin: '0 auto',
        maxWidth: 500,
      }}
    />
  );
}

export default (SearchBarReact);